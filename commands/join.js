const { SlashCommandBuilder } = require('discord.js');

module.exports = {
	data: new SlashCommandBuilder()
		.setName('join')
		.setDescription('Une el bot a un canal de Discord!')
        .setName('join'),
    async execute() {
    
    },
};